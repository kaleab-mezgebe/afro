import 'package:get/get.dart';

import '../views/portfolio_page.dart';

class PortfolioBinding extends Bindings {
  @override
  void dependencies() {
    // Add any dependencies needed for the portfolio page
    // For now, we'll keep it simple since the portfolio page doesn't need complex services
  }
}
